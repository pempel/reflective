{
  "name": "reflective",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "preinstall": "npm list grunt-cli -g || npm install grunt-cli -g"
  },
  "devDependencies": {
    "grunt": "^0.4.4",
    "grunt-contrib-compass": "^0.7.2",
    "grunt-contrib-uglify": "^0.4.0"
  }
}
